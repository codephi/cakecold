component{

    this.path = {
        webroot = ExpandPath( "./" ) & 'webroot',
        controller = ExpandPath( "./" ) & 'controller',
        view = ExpandPath( "./" ) & 'view'
    };

    this.controller = 'index';

    this.action = 'index';
}